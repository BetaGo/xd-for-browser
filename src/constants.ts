export enum DesignTool {
  Select,
  Rectangle,
  Ellipse,
  Polygon,
  Line,
  Pen,
  Text,
  Artboard,
  Zoom,
}

export enum OtherTool {
  Assets,
  Layers,
}

export enum MouseEventButton {
  Main,
  Auxiliary,
  Secondary,
  Fourth,
  Fifth,
}
